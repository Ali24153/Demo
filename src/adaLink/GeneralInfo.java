package adaLink;

public class GeneralInfo {

    //No, we cannot define a static constructor in Java, If we are trying to define a constructor with
    // the static keyword a compile-time error will occur
      // check benefit of abstract class(just the name of method without body
    // when we called from an extended class we can code it

    // check enum.
    // exclusive and inclusive
}
